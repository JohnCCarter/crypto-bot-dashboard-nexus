# Auto-activate trading bot environment
if [[ "$PWD" == *"crypto-bot-dashboard-nexus"* ]] && [[ "$VIRTUAL_ENV" == "" ]]; then
    if [[ -f "venv/bin/activate" ]]; then
        echo "🚀 Auto-aktiverar trading bot miljö..."
        source venv/bin/activate
    fi
fi