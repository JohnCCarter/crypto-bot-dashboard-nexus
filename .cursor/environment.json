{
  "install": "cd backend; pip install -r requirements.txt; cd ..; npm install",
  "terminals": [
    {
      "name": "Starta backend (Flask)",
      "command": "cd backend; flask run --host=0.0.0.0"
    },
    {
      "name": "Starta frontend (Vite)",
      "command": "npm run dev"
    }
  ]
}
